{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions for the viewer windows: the open dialog, window title/focus, spawning a window per document, and the drag-drop / open-file events.",
  "windows": [
    "main",
    "doc-*"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-set-title",
    "core:window:allow-set-focus",
    "core:window:allow-unminimize",
    "dialog:allow-open",
    "opener:allow-open-url",
    "window-state:default",
    "core:webview:allow-create-webview-window",
    "core:window:allow-close"
  ]
}
